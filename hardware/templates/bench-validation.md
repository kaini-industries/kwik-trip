# Bench validation template

- Unit, model, PCB revision, MCU suffix:
- Host environment and source revision:
- Adapter revision and measured supply:
- Battery disconnected and ground confirmed:
- Idle GPIO and unpowered-target behavior measured:
- Repeated raw chip IDs and status bytes:
- Cold power-cycle results:
- Clock/data/reset signal observations:
- Backup files, hashes, and acquisition procedure:
- Program/verify result (when implemented):
- Display/power/sleep observations (when implemented):
- Failures and unresolved assumptions:
- Evidence file paths and date:

Leave unperformed checks blank or explicitly untested. Compilation and simulated tests are not physical validation.
